import React, { Fragment } from "react";
import TodoSelection from "../components/Todos/todo-selection";

const Todos = () => (
    <Fragment>
        <TodoSelection />
    </Fragment>
)

export default Todos;